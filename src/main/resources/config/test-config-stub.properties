# ============================================================================
# RBA Task - 'stub' environment profile (classpath copy; activate with -Denv=stub)
#
# Deterministic offline API tier: the postman/REST exchange-rate tests run
# against recorded WireMock responses instead of the live RBA site, so they
# are instant, fully offline and never affected by the site's availability.
# Loaded on top of the base config by ConfigManager (12-factor profile overlay).
# ============================================================================

api.stub=true